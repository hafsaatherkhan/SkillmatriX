// import authFetch from "./authFetch";

// export const chatApi = {
//   initSession: (payload) => authFetch("/api/chat/session", { method: "POST", body: payload }),
//   fetchHistory: (id) => authFetch(`/api/chat/history/${id}`),
//   sendMessage: (sessionId, content) =>
//     authFetch("/api/chat/message", { method: "POST", body: { sessionId, content } }),
// };
